#include <cstdio>
#include <stdlib.h>
#include "ast.h"

// PROGRAM ID SEMICOLON varDec subProgList compStmt DOT
int main()
{
    ast = (N_PROG*) malloc(sizeof (N_PROG));
    ast->stmt = $6;
    ast->next = nullptr;
}
